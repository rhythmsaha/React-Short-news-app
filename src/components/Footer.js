import React from "react";
import classes from "./styles/Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.Footer}>
      <span>Clone Build by ViruS</span>
    </footer>
  );
};

export default Footer;
